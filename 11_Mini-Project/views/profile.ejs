<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini - Project</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div
      class="min-h-screen bg-gradient-to-br from-gray-700 to-gray-900 flex justify-center p-4"
    >
      <div class="container mx-auto">
        <div
          class="bg-white mx-auto shadow-xl rounded-2xl w-full max-w-md p-6 sm:p-8"
        >
          <h2 class="text-3xl font-extrabold text-center text-gray-800 mb-6">
            User Profile
          </h2>
          <div class="flex items-center justify-between">
            <a href="/profile/upload" class="underline underline-offset-5 text-blue-500">Upload Profile Pic</a>
            <!-- Logout Button -->
            <a
              href="/logout"
              class="w-auto cursor-pointer px-3 py-2 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg shadow-md transition-all duration-200"
              >Logout</a
            >
          </div>
          <div class="flex flex-column">
            <div class="space-y-3">
              <div class="flex justify-between items-center mt-5">
                <h3 class="text-2xl font-semibold text-gray-800">
                  <span class="font-bold">Name: </span><%= user.name %>
                </h3>
                <img
                  src="/images/uploads/<%= user.profilePic %>"
                  alt="Profile Picture"
                  class="w-10 h-10 rounded-full object-cever"
                />
              </div>
              <form method="post" action="/post">
                <!-- Title -->
                <div>
                  <label
                    for="title"
                    class="block text-xl font-bold text-gray-700 mb-1"
                    >Title:</label
                  >
                  <input
                    type="text"
                    id="title"
                    name="title"
                    required
                    placeholder="Enter the post title"
                    class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
                  />
                </div>
                <!-- Content -->
                <div>
                  <label class="block text-xl font-bold text-gray-700 pt-2 mb-1"
                    >Add Post:</label
                  >
                  <textarea
                    name="content"
                    cols="100"
                    placeholder="write a post"
                    class="w-full px-4 mt-2 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
                    required
                  ></textarea>
                  <!-- Submit Button -->
                  <input
                    type="submit"
                    value="Create Post"
                    class="w-full mt-3 cursor-pointer bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-lg shadow-md transition-all duration-200"
                  />
                </div>
              </form>
            </div>
          </div>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 my-5">
          <% user.post.reverse().forEach((post) => { %>
          <div
            class="bg-white mt-5 px-5 py-3 flex flex-col space-y-3 rounded-lg"
          >
            <h2 class="text-2xl font-semibold text-gray-800">
              <span class="font-bold">Username:</span> <%= user.username %>
            </h2>
            <h2 class="text-2xl font-semibold text-gray-800">
              <span class="font-bold">Post Title:</span> <%= post.title %>
            </h2>
            <p class="text-xl text-gray-800">
              <span class="text-2xl font-bold">Post Content:</span> <%=
              post.content %>
            </p>
            <div class="flex justify-center mt-5 gap-3">
              <a
                href="/edit/<%= post._id %>"
                class="w-auto cursor-pointer px-3 py-2 bg-green-600 hover:bg-green-700 text-white font-semibold rounded-lg shadow-md transition-all duration-200"
                >Edit</a
              >
              <a
                href="/delete/<%= post._id %>"
                class="w-auto cursor-pointer px-3 py-2 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg shadow-md transition-all duration-200"
                >Delete</a
              >
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
